# This workflow will execute the terraform script to deploy to aws 
name: Deploy to AWS

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

env:
  AWS_ACCESS_KEY_ID: AKIA47CRVQOIPAA3EDHF
  AWS_SECRET_ACCESS_KEY: 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
  
jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest

    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Deploy
      run: |-
        chmod +x deploy.sh
        ./deploy.sh
      